<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence" 
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <!-- Definição da unidade de persistência -->
    <persistence-unit name="java" transaction-type="RESOURCE_LOCAL">

        <!-- Provedor de persistência (Hibernate neste caso) -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Mapeamento das classes de entidade -->
        <class>com.example.model.MyEntity</class>

        <!-- Propriedades de conexão com o banco de dados -->
        <properties>
            <!-- Driver JDBC do MySQL -->
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>

            <!-- URL de conexão com o banco de dados MySQL -->
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3307/locadora?useSSL=false&amp;serverTimezone=UTC"/>

            <!-- Usuário do banco de dados MySQL -->
            <property name="javax.persistence.jdbc.user" value="root"/>

            <!-- Senha do usuário do banco de dados MySQL -->
            <property name="javax.persistence.jdbc.password" value=""/>

            <!-- Dialeto do Hibernate para o MySQL 8 -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>

            <!-- Mostrar SQL gerado pelo Hibernate -->
            <property name="hibernate.show_sql" value="false"/>

            <!-- Formatar SQL gerado pelo Hibernate -->
            <property name="hibernate.format_sql" value="true"/>

            <!-- Atualizar o esquema do banco de dados automaticamente (pode ser perigoso em produção deve ser usado o "validade") -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
        </properties>
    </persistence-unit>
</persistence>
